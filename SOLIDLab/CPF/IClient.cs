namespace ConsoleApp1.CPF
{
    /**
     * Use of interface so that implementing class is
     * open to extension but closed for modification
     *
     * However, IClient class currently does not have
     * functions so the interface is empty.
     */
    interface IClient
    {
    }
}
